
/**********************************************************************************

	[5] Token_stream::get() 함수를 수정하여 줄바꿈(newline)을 감지하면 
	Token(print)를 반환하도록 하라.이는 공백 문자(whitespace character)를 탐지하고 
	줄바꿈 문자('\n')를 특별히 처리해야 함을 의미한다.
	표준 라이브러리 함수 isspace(ch)는 ch가 공백 문자일 경우 
	true를 반환하므로 유용할 수 있다.

**********************************************************************************/