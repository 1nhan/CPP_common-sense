/**********************************************************************************
* 
	[4] get_value(), set_value(), is_declared(), define_name() 함수는 
	모두 변수 테이블 var_table을 대상으로 작동한다.
	var_table을 vector<Variable> 타입의 멤버로 갖는 Symbol_table 클래스를 정의하고, 
	멤버 함수로 get(), set(), is_declared(), declare()를 포함하라.
	계산기를 Symbol_table 타입의 변수를 사용하도록 재작성하라.

**********************************************************************************/